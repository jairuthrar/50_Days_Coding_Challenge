'''a binary string consisting of only zeros and ones. 
Your code should find the biggest number of 
consecutive zeros in the string. For example, given the string'''

user_input=input("enter string: ")
lst=list(user_input.split("1"))
ans=max(lst,key=len)
print("count of longest consecutive zeros:", len(ans))

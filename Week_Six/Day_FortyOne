#swap characters pairwise between the two halves of a given string
s = input("Enter string : ")
new_string =""
for i in range(len(s)//2):
    new_string+=(s[i]+s[i+len(s)//2])
print("String After Swapping:",new_string)
